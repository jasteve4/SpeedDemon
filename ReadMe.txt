first thing to do is to add the .ssh file to settings

if not on computer than do a "git clone --recursive git@github.com:jasteve4/SpeedDemon.git"


before starting coding on bot do a "git pull"

when done do:

git status
git add <files>
git commit -m <"message">
git push

